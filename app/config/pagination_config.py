class PaginationConfig:
    """
    Sayfalama için varsayılan ayarları tutan yapı.
    
    """
    DEFAULT_LIMIT = 10      # Sayfa başına varsayılan kayıt sayısı
    DEFAULT_OFFSET = 0      # Varsayılan offset değeri
    DEFAULT_PAGE = 1        # Varsayılan sayfa numarası
    MAX_LIMIT = 100         # Maksimum kayıt limiti
